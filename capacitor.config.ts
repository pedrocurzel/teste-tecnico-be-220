import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'teste técnico be220',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
